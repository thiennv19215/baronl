# Source UI screenshots — not provided

Không có screenshot hoặc executable UI nguồn trong đầu vào ngày 2026-08-10. Thư mục này cố ý không chứa ảnh giả/thay thế; ma trận đối chiếu dùng trạng thái `BLOCKED_SOURCE`.

Chỉ thêm screenshot nguồn khi chủ dự án xác nhận quyền dùng cho mục đích tham chiếu. Mỗi ảnh cần ghi app version, screen/state, viewport, Windows DPI/scaling, ngày chụp và phạm vi quyền. Không ship ảnh nguồn trong installer nếu quyền chỉ cho review nội bộ.
